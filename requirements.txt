# This project runs entirely on Python's Standard Library (urllib, sqlite3, json, math, etc.)
# No external library dependencies are required!
# This keeps the Docker image tiny, secure, and extremely fast to build.
